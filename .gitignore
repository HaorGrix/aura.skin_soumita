# Logs
logs
*.log
npm-debug.log*

# Dependencies
node_modules
dist
dist-ssr
*.local

# Environment variables (Supabase keys, etc.) — *.local above already covers
# .env.local / .env.*.local; this explicitly covers a bare ".env" too.
.env
.env.*
!.env.example

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*

# Design-tool scratch (22 MB of .dc.html project files, screenshot scraps and
# thumbnails). Not app source — the finished art lives in assests/cate/.
# Committing it would bloat the repo permanently.
category-row-with-pink-tiles/

# Supabase CLI local state (project link cache) — machine-specific, not source.
supabase/.temp/
